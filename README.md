## Step-by-step DMS cluster setup

In project folder:

```
source /opt/anaconda/anaconda3/etc/profile.d/conda.sh
conda init bash


conda create -p /NOBACKUP/aumona/projects/RF-MALI/.condaenv python=3.10.4
conda activate /NOBACKUP/aumona/projects/RF-MALI/.condaenv
```
