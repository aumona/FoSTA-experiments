forestkernel @ git+https://github.com/jakerhodes/RF-GAP-Python@forestkernel-wip
demap @ git+https://github.com/scottgigante/DEMaP
torch
aeon==1.4.0
phate
umap-learn[plot]  # pyliger imports umap.plot
jax==0.4.35   # for CPU only on local MacOS device
# jax[cuda12]  # NVIDIA GPU support on cluster
pot
scanpy
matplotlib
pandas
ipykernel
rpy2  # for splatter
psutil

# Lung batch experiment dependencies.
scanorama==1.7.4
scib-metrics==0.5.6
scvi-tools==1.3.3
pyliger==0.2.4
# Avoid legacy plotting packages that cannot import with current Bokeh.
holoviews>=1.18
panel>=1
# Keep LIGER's louvain and Leiden bindings on the same igraph release family.
igraph>=0.10,<0.11
python-igraph>=0.10,<0.11
leidenalg>=0.10,<0.11
louvain>=0.8,<0.9
# louvain imports pkg_resources, removed in setuptools 81.
setuptools<81
